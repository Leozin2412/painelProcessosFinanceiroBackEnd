-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public._prisma_migrations (
  id character varying NOT NULL,
  checksum character varying NOT NULL,
  finished_at timestamp with time zone,
  migration_name character varying NOT NULL,
  logs text,
  rolled_back_at timestamp with time zone,
  started_at timestamp with time zone NOT NULL DEFAULT now(),
  applied_steps_count integer NOT NULL DEFAULT 0,
  CONSTRAINT _prisma_migrations_pkey PRIMARY KEY (id)
);
CREATE TABLE public.base_processos (
  ident_sinistro bigint NOT NULL,
  codigo_sinistro text NOT NULL UNIQUE,
  data_criacao_sinistro timestamp with time zone,
  regulador_situacao text,
  numero_seguradora text,
  operacao text,
  segurado text,
  situacao text,
  data_ultima_situacao timestamp with time zone,
  total_despesas numeric DEFAULT 0.00,
  total_honorarios numeric DEFAULT 0.00,
  endereco text,
  tem_vistoria_agendada boolean DEFAULT false,
  data_vistoria timestamp with time zone,
  dat_modificacao_sinistro timestamp with time zone,
  criado_em timestamp with time zone DEFAULT now(),
  atualizado_em_nosso_banco timestamp with time zone DEFAULT now(),
  CONSTRAINT base_processos_pkey PRIMARY KEY (ident_sinistro)
);
CREATE TABLE public.despesas (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  ident_sinistro integer NOT NULL,
  dados_gclaims jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  ident_despesa bigint UNIQUE,
  CONSTRAINT despesas_pkey PRIMARY KEY (id)
);
CREATE TABLE public.drl_documentos (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  ident_sinistro text NOT NULL,
  categoria text NOT NULL,
  item_ordem text,
  documento_nome text NOT NULL,
  status USER-DEFINED NOT NULL DEFAULT 'Pendente'::drl_documento_status,
  data_solicitacao date,
  data_entrega date,
  data_ultima_reiteracao date,
  observacoes text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT drl_documentos_pkey PRIMARY KEY (id)
);
CREATE TABLE public.drl_templates (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  acionamento text NOT NULL,
  categoria text NOT NULL,
  documento_nome text NOT NULL,
  obrigatorio boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT drl_templates_pkey PRIMARY KEY (id)
);
CREATE TABLE public.faturamentos (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  ident_sinistro integer NOT NULL,
  dados_gclaims jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  ident_faturamento bigint UNIQUE,
  CONSTRAINT faturamentos_pkey PRIMARY KEY (id)
);
CREATE TABLE public.funcionarios (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  nome text NOT NULL,
  local text,
  data_nascimento date NOT NULL,
  cargo text,
  foto_url text,
  ativo boolean DEFAULT true,
  CONSTRAINT funcionarios_pkey PRIMARY KEY (id)
);
CREATE TABLE public.honorarios (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  ident_sinistro integer NOT NULL,
  dados_gclaims jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  ident_honorario bigint UNIQUE,
  CONSTRAINT honorarios_pkey PRIMARY KEY (id)
);
CREATE TABLE public.honorarios_seguradora (
  id integer NOT NULL DEFAULT nextval('honorarios_seguradora_id_seq'::regclass),
  seguradora text,
  valor_atual numeric,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT honorarios_seguradora_pkey PRIMARY KEY (id)
);
CREATE TABLE public.noticias (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  titulo text NOT NULL,
  resumo text,
  conteudo text,
  categoria text DEFAULT 'MERCADO'::text,
  imagem_url text,
  link_original text UNIQUE,
  data_publicacao timestamp with time zone DEFAULT now(),
  created_at timestamp with time zone DEFAULT now(),
  url_externa text,
  video_url text,
  url_link text NOT NULL,
  url_video text,
  criado_por uuid,
  CONSTRAINT noticias_pkey PRIMARY KEY (id),
  CONSTRAINT noticias_criado_por_fkey FOREIGN KEY (criado_por) REFERENCES auth.users(id)
);
CREATE TABLE public.perfis (
  id uuid NOT NULL,
  email text NOT NULL,
  role text DEFAULT 'operacao'::text CHECK (role = ANY (ARRAY['operacao'::text, 'coordenador'::text, 'diretoria'::text])),
  CONSTRAINT perfis_pkey PRIMARY KEY (id),
  CONSTRAINT perfis_id_fkey FOREIGN KEY (id) REFERENCES auth.users(id)
);
CREATE TABLE public.processo_documentos (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  processo_id bigint NOT NULL,
  doc_solicitado text NOT NULL,
  status USER-DEFINED NOT NULL DEFAULT 'Pendente'::processo_documento_status,
  data_solicitacao date,
  data_entrega date,
  data_reiteracao date,
  observacoes text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  url_drive text,
  categoria text NOT NULL DEFAULT 'PREJUÍZO'::text,
  CONSTRAINT processo_documentos_pkey PRIMARY KEY (id),
  CONSTRAINT processo_documentos_processo_id_fkey FOREIGN KEY (processo_id) REFERENCES public.sinistros(ident_sinistro)
);
CREATE TABLE public.sinistro_interacoes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  ident_sinistro bigint NOT NULL,
  assunto text NOT NULL DEFAULT ''::text,
  corpo_texto text NOT NULL DEFAULT ''::text,
  tipo_origem text NOT NULL,
  remetente text,
  data_cabecalho_email text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  message_id text,
  CONSTRAINT sinistro_interacoes_pkey PRIMARY KEY (id),
  CONSTRAINT sinistro_interacoes_ident_sinistro_fkey FOREIGN KEY (ident_sinistro) REFERENCES public.sinistros(ident_sinistro)
);
CREATE TABLE public.sinistros (
  ident_sinistro bigint NOT NULL,
  ident_compania bigint,
  nro_correlativo integer,
  codigo_sinistro text,
  causa_sinistro text,
  ident_seguradora bigint,
  ident_apolice bigint,
  ident_operacao bigint,
  ident_cobertura bigint,
  ident_segurado bigint,
  ident_corretora bigint,
  ident_regulador bigint,
  ident_regulador_principal bigint,
  ident_coordenador bigint,
  ident_contato_seguradora bigint,
  ident_contato_corretora bigint,
  ident_situacao bigint,
  ident_ramo bigint,
  ident_tipo_vistoria bigint,
  data_ocorrencia timestamp with time zone,
  data_aviso_seguradora timestamp with time zone,
  data_entrada timestamp with time zone,
  dat_criacao_sinistro timestamp with time zone,
  dat_modificacao_sinistro timestamp with time zone,
  dat_ultima_situacao timestamp with time zone,
  vigencia_inicial timestamp with time zone,
  vigencia_final timestamp with time zone,
  data_vistoria timestamp with time zone,
  data_conclusao_vistoria timestamp with time zone,
  data_ultimo_documento_recebido timestamp with time zone,
  data_primeiro_contato timestamp with time zone,
  data_envio_drl timestamp with time zone,
  data_ultima_reiteracao timestamp with time zone,
  regulador text,
  regulador_principal text,
  coordenador text,
  contato_seguradora text,
  email_contato_seguradora text,
  telefones_contato_seguradora text,
  contato_corretora text,
  telefone1_contato_corretora text,
  telefone2_contato_corretora text,
  email_contato_corretora text,
  cargo_contato_corretora text,
  nro_segurado text,
  nro_seguradora text,
  nro_corretora text,
  nro_aviso text,
  nro_apolice text,
  relat_preliminar_finalizado timestamp with time zone,
  relat_preliminar_enviado_seguradora timestamp with time zone,
  relat_final_finalizado timestamp with time zone,
  relat_final_enviado_seguradora timestamp with time zone,
  operacao text,
  cobertura text,
  coberturas_adicionais text,
  seguradora text,
  segurado text,
  corretora text,
  estipulante text,
  loja_franqueado_produto text,
  situacao text,
  ramo text,
  valor_is numeric,
  valor_franquia numeric,
  descricao_franquia text,
  carencia text,
  descricao_natureza_danos text,
  endosso text,
  condicoes_gerais text,
  valor_reclamado numeric,
  valor_indenizacao numeric,
  valor_reserva_seguradora numeric,
  estimativa_prejuizo numeric,
  estimativa_honorario numeric,
  total_despesas numeric,
  total_honorarios numeric,
  local_vistoria text,
  estado text,
  sigla_estado text,
  cidade text,
  cep text,
  endereco text,
  bairro text,
  numero text,
  complemento text,
  informacoes_adicionais text,
  local_risco text,
  estado_local_risco text,
  sigla_estado_local_risco text,
  cidade_local_risco text,
  cep_local_risco text,
  endereco_local_risco text,
  bairro_local_risco text,
  numero_local_risco text,
  complemento_local_risco text,
  informacoes_adicionais_local_risco text,
  tem_vistoria_agendada boolean,
  descricao_vistoria text,
  prestador_servico text,
  periodo_vistoria text,
  tipo_vistoria text,
  ultima_observacao_processo text,
  historico_observacoes text,
  judicial boolean,
  salvados boolean,
  ressarcimento boolean,
  sem_cobertura boolean,
  suspeita_fraude boolean,
  prejuizo_inferior_franquia boolean,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  uf_dashboard character varying,
  dados_gclaims jsonb DEFAULT '{}'::jsonb,
  pontos_importantes jsonb DEFAULT '[]'::jsonb,
  historico_drl jsonb DEFAULT '[]'::jsonb,
  data_primeira_solicitacao_documentos timestamp with time zone,
  data_ultima_manifestacao_segurado timestamp with time zone,
  data_conclusao_relatorio_vistoria timestamp with time zone,
  url_drive_pasta text,
  drive_folder_id text,
  drive_url text,
  chat_url text,
  link_google_chat text,
  drive_financeiro_url text,
  CONSTRAINT sinistros_pkey PRIMARY KEY (ident_sinistro)
);
CREATE TABLE public.status_processos_bh (
  id integer NOT NULL DEFAULT nextval('status_processos_bh_id_seq'::regclass),
  codigo_sinistro character varying UNIQUE,
  status_bh character varying DEFAULT NULL::character varying CHECK (status_bh IS NULL OR (status_bh::text = ANY (ARRAY['ok'::character varying, 'revisado'::character varying, 'pedido de autorização'::character varying, 'corrigir'::character varying, 'nf emitida'::character varying]::text[]))),
  ultima_atualizacao timestamp with time zone DEFAULT now(),
  obs text,
  CONSTRAINT status_processos_bh_pkey PRIMARY KEY (id)
);
CREATE TABLE public.timesheets (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  ident_sinistro bigint NOT NULL,
  codigo_sinistro text,
  regulador_prestador text,
  tp_incidencia text,
  dt_inicial timestamp with time zone,
  total_decimal numeric,
  descricao_tarefa text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  ident_timesheet bigint UNIQUE,
  dados_gclaims jsonb DEFAULT '{}'::jsonb,
  dt_final timestamp with time zone,
  total_minutos integer,
  hash_unico text UNIQUE,
  ident_compania bigint,
  nro_correlativo integer,
  ident_seguradora bigint,
  seguradora text,
  dat_criacao_sinistro timestamp with time zone,
  data_ocorrencia timestamp with time zone,
  ident_operacao bigint,
  operacao text,
  ident_cobertura bigint,
  cobertura text,
  ident_incidencia bigint,
  ident_tipo_incidencia bigint,
  incidencia text,
  possui_cobranca boolean,
  cobrado boolean,
  dt_lancamento timestamp with time zone,
  unidade_cobranca text,
  valor_por_hora numeric,
  valor_cobrado numeric,
  tipo_pessoa text,
  ident_regulador_prestador bigint,
  honorario_gerado boolean,
  honorario_faturado boolean,
  sinistros_fechados boolean,
  sinistros_cancelados boolean,
  tipo_timesheet text,
  subtipo_timesheet text,
  descricao_timesheet text,
  data_criacao_hon timestamp with time zone,
  quantidade numeric,
  valor_unitario numeric,
  valor_ajuste numeric,
  data_despesa timestamp with time zone,
  nro_documento text,
  nro_erp text,
  nro_nota text,
  taxa numeric,
  valor_bruto numeric,
  valor_liquido numeric,
  valor_nota numeric,
  foi_cobrado boolean,
  data_cobranca timestamp with time zone,
  foi_pago boolean,
  data_pagamento timestamp with time zone,
  descricao_fatura text,
  valor_ajuste_fatura numeric,
  data_criacao timestamp with time zone,
  numero_aviso text,
  data_modificacao_timesheet timestamp with time zone,
  CONSTRAINT timesheets_pkey PRIMARY KEY (id)
);
CREATE TABLE public.vistorias (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  ident_sinistro integer NOT NULL,
  dados_gclaims jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  ident_vistoria bigint UNIQUE,
  CONSTRAINT vistorias_pkey PRIMARY KEY (id)
);
